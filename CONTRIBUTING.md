# Contributing

## Bug reports

[File an issue](https://github.com/theodorejb/es-cookie/issues) with a reproducible example of the problem.

## Testing

After cloning the repository, run `npm install && npm run prepare`.
Then run `npm run test:browser` to execute the tests in Firefox.
